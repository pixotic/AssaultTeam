using UnityEngine;
using System.Collections;

public class EntitySpellManager : MonoBehaviour {
    /*
     * The Entity Spell Manager is the class which handles an entity's spells
     * Which spells it knows, specific configurations and so on.

     * The extended versions of the Entity Spell Manager (For Heroes, Monsters, NPCs etc) 
     * will allow for the actual in-game use of the spells, driven either by AI or player control.
    */

	// Use this for initialization
	void Start () {
	
	}
	
	// Update is called once per frame
	void Update () {
	
	}
}
