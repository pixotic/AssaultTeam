using UnityEngine;
using System.Collections;

public class EntityBehaviourManager : MonoBehaviour {
    /*
     * The Entity Behaviour Manager serves as the main class for 
     * dictating an entity's movement and actions
     * 
     * Basic attributes and functionality goes here, with specific 
     * other aspects being handled by extended versions of the 
     * behaviour manager.
     * 
     * For example, AI won't be handled specifically in this class, 
     * that belongs to EntityAIBehaviourManager. Similarly, Heroes don't need
     * aggro tables and the associated AI/mechanics, so that will go in
     * the EntityMonsterBehaviourManager
     */


	// Use this for initialization
	void Start () {
	
	}
	
	// Update is called once per frame
	void Update () {
	
	}
}
